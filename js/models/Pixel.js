site.models.Pixel = Backbone.Model.extend({

    //default for Pixel model, refer to this if no other reference is available
    defaults: {
        id: "0_0",
        x: 0,
        y: 0,
        hidden: false,
        color: "white"

    }

});
